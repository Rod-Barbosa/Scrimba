# React.memo() as a substitute for componentShouldUpdate

### note that changing the Grandparent component doesn't cause the children to rerender. And no PureComponent is used, only React.memo()

![](./start.png)
